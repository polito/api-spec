npm run compile all

CLIENT_DIFFS=$(git diff --name-only dist/clients)
if [[ $CLIENT_DIFFS ]]; then
  echo "OpenAPI spec is out of date. Add your OpenAPI file(s) to the commit:"
  echo "$CLIENT_DIFFS" 
  exit 1;
fi
